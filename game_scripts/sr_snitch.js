import {ServerConfig} from 'bot.js';
const Discord = require('discord.js');

class SRSnitch{
    constructor(client, guild, config){
        this.client = client;
        this.guild = guild;
        this.config = config;
    }
}